# 🇨🇭 SwissGate Backend Core
Ecosistema Financiero y Operativo de Privacidad Absoluta con Motor IA Qwen.

## Despliegue
1. Clona el repositorio.
2. Instala dependencias: `npm install`
3. Crea un archivo `.env` basado en `.env.example`.
4. Ejecuta: `npm start`
